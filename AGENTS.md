# Backoffice Codex 작업 지침

이 문서는 이 저장소에서 Codex가 따르는 최상위 프로젝트 지침이다. `.cursor/`는 Cursor 호환 자산이며 Codex의 규칙 소스가 아니다. Codex는 이 문서와 `.agents/skills/`를 기준으로 작업하고, 기존 구현 관례를 확인할 때만 `.cursor/rules/`를 참고 자료로 사용한다.

## 작업 원칙

- 사용자 요청의 결과를 먼저 정의하고 필요한 범위만 변경한다.
- 기능 안정성, 기존 동작 보존, 가독성, 유지보수성 순으로 판단한다.
- 기존 구조, 네이밍, 공용 컴포넌트와 자산을 새 구현보다 우선 재사용한다.
- 설명·검토·진단 요청에는 파일을 수정하지 않는다. 수정 의도가 명확한 요청은 불필요한 재확인 없이 구현하고 검증한다.
- 선택에 따라 결과가 크게 달라지거나 데이터 손실, 보안, 외부 시스템 변경이 발생할 때만 사용자 확인을 요청한다.
- 사용자 요청 밖의 리팩터링, 디자인 변경, 패키지 추가, 데이터 변경을 끼워 넣지 않는다.
- 비밀값은 출력하거나 코드에 하드코딩하거나 커밋하지 않는다.
- 커밋, 푸시, PR 생성·병합은 사용자가 명시적으로 요청한 경우에만 수행한다.

## 작업 시작 절차

1. `git status --short`로 기존 변경을 확인하고 사용자 변경을 보존한다.
2. 대상 파일과 인접 구현을 검색해 현재 패턴, 호출 관계, 테스트 위치를 파악한다.
3. 변경 전 성공 조건과 회귀 위험을 짧게 정리한다.
4. 작업과 일치하는 `.agents/skills/*/SKILL.md`가 있으면 전체를 읽고 적용한다.
5. 여러 디렉터리에 `AGENTS.md` 또는 `AGENTS.override.md`가 있으면 대상 파일에 가장 가까운 지침을 우선한다.

## ASEIC 관리자 구현 기준

관리자 기능 개발에는 다음 자료를 서로 다른 목적으로 사용한다.

- 기능 기획서: `docs/ASEIC글로벌포럼홈페이지_관리자_화면설계서_ver1_0_HK20260730.pptx`
- 목록 UI 기준: `resources/views/backoffice/members/index.blade.php`
- 등록 UI 기준: `resources/views/backoffice/members/create.blade.php`
- 수정 UI 기준: `resources/views/backoffice/members/edit.blade.php`
- 폼 구조 기준: `resources/views/backoffice/members/_form.blade.php`
- 공용 CRUD 스타일: `public/css/backoffice/backoffice-crud.css`

자료의 역할과 우선순위를 혼합하지 않는다.

### 기능과 데이터

- ASEIC 프로젝트 전용 메뉴는 기획서의 필드, 필수 여부, 상태값, 검색 조건, 연결 관계, 업무 흐름을 구현 기준으로 삼는다.
- 기획서의 화면 그림에서 기능을 추측하지 말고 슬라이드 설명, 데이터 관계, 인접 화면 흐름을 함께 확인한다.
- 기획서가 불명확하거나 서로 모순되면 관련 슬라이드 번호와 쟁점을 제시하고 구현 전에 확인한다.
- 기획서에 없는 필드나 상태를 편의상 추가하지 않는다. 기술적으로 필요한 내부 값은 사용자 UI와 분리한다.

### 공통 운영 모듈

- 홈페이지 기본 설정, 팝업 관리, 배너 관리, 관리자 관리, 권한그룹·권한 관리는 현재 개발된 모듈과 동작을 우선한다.
- 위 모듈은 기획서 화면에 맞추기 위해 구조를 재작성하거나 기능을 축소하지 않는다.
- 프로젝트 전용 기능과 연결하는 데 필요한 최소 확장만 허용하며 기존 CRUD, 권한, 정렬, 노출, 파일 처리 흐름을 보존한다.

### 디자인과 화면 구성

- 기획서의 색상, 버튼 모양, 버튼 위치, 필터 배치, 여백, 카드·테이블 형태는 디자인 기준으로 사용하지 않는다.
- 관리자 목록은 `backoffice/members`의 헤더 버튼 순서, 필터 영역, Total·표시 개수, 테이블, 관리 버튼, pagination 구조를 따른다.
- 등록·수정은 `backoffice/members/create`, `edit`, `_form`의 목록 버튼, 섹션 제목, 라벨·필드 행, 필수 표시, 저장·취소·삭제 액션 구조를 따른다.
- 버튼 색상, 크기, 아이콘과 의미는 기존 관리자에서 같은 행위를 하는 버튼을 찾아 그대로 재사용한다.
- 새 CSS나 임의 색상·간격을 만들기 전에 `backoffice-crud.css`와 기존 `board-*`, `bo-*`, `btn-*` 클래스로 해결한다.
- 도메인 때문에 필요한 예외 UI가 있으면 기존 패턴으로 표현할 수 없는 이유를 먼저 설명하고 최소 범위로 추가한다.

### 구현 전 대조표

프로젝트 전용 메뉴를 구현하기 전에 다음을 구분한다.

| 판단 대상 | 기준 |
|---|---|
| 필드, 필수값, 상태, 검색 조건 | 기획서 |
| 저장·노출·연결·선택 흐름 | 기획서 |
| 공통 설정·팝업·배너·관리자·권한 동작 | 현재 구현 |
| 목록·등록·수정 화면 구조 | `members` 화면 |
| 버튼 위치·색상·크기·아이콘 | 동일 행위의 기존 관리자 UI |
| CSS·JavaScript 패턴 | 기존 공용 자산 |

구현 완료 후에는 기획서 요구 항목 누락과 `members` UI 패턴 이탈을 별개의 검증 항목으로 점검한다.

## Laravel 구현 기준

- Controller는 요청 조정과 응답 반환에 집중한다.
- 입력 검증과 권한 확인은 기존 Form Request 및 Policy 패턴을 우선한다.
- 재사용되는 비즈니스 로직과 트랜잭션 경계는 `app/Services/`에 둔다.
- Model은 관계, 스코프, 캐스트, 액세서·뮤테이터 중심으로 유지한다.
- 관계 조회 변경 시 N+1 가능성을 확인하고 필요한 관계만 eager loading한다.
- Blade에는 표시를 위한 단순 조건과 반복만 둔다.
- 예외 처리와 응답 형식은 인접 기능의 기존 패턴을 따른다.

## Blade와 정적 자산

- Blade에 새 인라인 `<style>`, `<script>`, `style=`, `onclick=`을 추가하지 않는다.
- CSS는 `public/css/`, JavaScript는 `public/js/`에서 관리한다.
- 기존 partial, component, 공용 클래스와 공용 스크립트를 먼저 재사용한다.
- 백오피스 CRUD 화면은 `resources/views/backoffice/board-posts/notices`와 동일 도메인의 인접 화면을 함께 비교한다.
- 공용 레이아웃 클래스는 `bo-*` 접두사를 사용하고 도메인 전용 클래스는 필요한 경우에만 만든다.
- route 이름, Controller 메서드, 폼 action, JavaScript endpoint를 한 흐름으로 대조한다.

## 데이터베이스 안전

- 스키마와 데이터 변경은 영향 범위와 기존 데이터 보존 여부를 먼저 확인한다.
- `migrate:fresh`, `migrate:reset`, `migrate:rollback`, 대량 삭제는 사용자의 명시적 승인 없이 실행하지 않는다.
- 일반 `migrate`도 실제 데이터베이스에 영향을 주므로 사용자가 실행을 요청했거나 승인한 경우에만 실행한다.
- 마이그레이션 파일 작성과 실행을 구분한다. 실행하지 않았으면 완료 보고에 명시한다.
- 시더는 반복 실행 가능성과 기존 데이터 삭제 여부를 확인한다.

## 개발 환경과 명령 실행

- 기본 환경은 Ubuntu, Docker, Laravel Sail, MySQL이다.
- 명령 전 컨테이너 상태와 `vendor/bin/sail` 존재 여부를 확인한다.
- 컨테이너가 실행 중이면 PHP, Artisan, Composer 명령은 Sail을 우선한다. 실행되지 않았거나 Sail을 사용할 수 없으면 로컬 도구 사용 가능 여부를 확인한다.
- `.env` 값은 필요한 키의 존재와 연결 관계만 확인하고 비밀값은 출력하지 않는다.
- 설정 스크립트의 성공 문구보다 실제 종료 코드와 후속 검증 결과를 신뢰한다.

## 검증 하네스

- 코드 변경 후 `backoffice-self-verification-harness` 스킬을 기본 검증 절차로 사용한다.
- 모든 검증을 무조건 실행하지 않고 변경 경로와 위험에 연결된 최소 충분 검증을 선택한다.
- 정적 검사, 대상 테스트, 런타임 시나리오 순으로 좁게 시작하고 실패 원인을 해결한 뒤 범위를 넓힌다.
- 테스트 실패가 기존 실패인지 변경으로 인한 실패인지 근거를 남긴다.
- 실행하지 않은 검증은 통과로 표현하지 않는다.

최종 보고에는 다음을 포함한다.

- 변경 결과와 주요 파일
- 실행한 검증 명령 또는 시나리오와 결과
- 미실행 검증, 실패 항목, 남은 위험
- 사용자가 바로 수행할 다음 단계가 있을 때만 해당 명령

## 프로젝트 스킬

Codex용 스킬의 공식 진입점은 `.agents/skills/`다.

- `backoffice-menu-scaffold`: 신규 백오피스 CRUD 메뉴와 화면 구성
- `backoffice-self-verification-harness`: 변경 범위 기반 검증과 증거 보고
- `laravel-refactor-checklist`: Laravel 계층 리팩터링
- `rules-conflict-audit`: 프로젝트 규칙의 중복과 충돌 감사

`.agents/skills`가 `.cursor/skills`를 가리키는 심볼릭 링크여도 Codex에서는 `.agents/skills` 경로를 사용한다. 스킬을 적용할 때는 해당 `SKILL.md` 전체를 먼저 읽는다.

## 서브에이전트

- 서브에이전트는 사용자가 병렬 작업, 위임, 서브에이전트 사용을 명시적으로 요청한 경우에만 사용한다.
- 사용할 때는 서로 독립적인 읽기·검증 작업으로 범위를 나누고 같은 파일을 동시에 수정하지 않는다.
- 주 에이전트가 결과를 직접 검토하고 최종 검증과 통합을 책임진다.

## 규칙 우선순위

충돌 시 다음 순서를 따른다.

1. 시스템·개발자·사용자의 현재 지시
2. 대상 파일에 가장 가까운 `AGENTS.override.md` 또는 `AGENTS.md`
3. 저장소 루트의 이 문서
4. 현재 작업에 적용된 `.agents/skills/` 지침
5. 기존 코드 관례와 참고용 `.cursor/rules/`

상위 지침과 충돌하는 하위 규칙이나 참고 문서는 적용하지 않는다.
